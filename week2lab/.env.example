# OpenAI API Configuration
# Replace with your actual API key, or leave empty to use Mock LLM mode
OPENAI_API_KEY=

# Set to "true" to force Mock LLM mode (no API calls)
MOCK_MODE=true

# Server port (default: 3000)
PORT=3000
